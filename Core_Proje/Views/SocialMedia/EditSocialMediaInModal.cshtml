@model EntitiyLayer.Concrete.SocialMedia

@using (Html.BeginForm("EditSocialMediaInModal", "SocialMedia", FormMethod.Post))
{
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title"> Sosyal Hesap Düzenle </h4>
                <form class="forms-sample">
                    @*<div class="form-group">
                <label>ID</label>
                @Html.TextBoxFor(x=> x.SocialMediaID, new {@class="form-control"})
                </div>*@
                    @Html.HiddenFor(x=>x.SocialMediaID)
                    @Html.HiddenFor(x=>x.Icon)
                    @Html.HiddenFor(x=>x.Name)
                    @*<div class="form-group">
                <label>Başlık</label>
                @Html.TextBoxFor(x=> x.Name, new {@class="form-control"})
                </div>*@
                    <div class="form-group">
                        <label>Url</label>
                        @Html.TextBoxFor(x=> x.Url, new {@class="form-control text-light"})
                    </div>
                    @*<div class="form-group">
                <label>İkon</label>
                @Html.TextBoxFor(x=> x.Icon, new {@class="form-control"})
                </div>*@
                    <button id='modalClose' type="submit" class="btn btn-outline-primary btn-sm" data-bs-dismiss="modal">Kaydet</button>
                </form>
            </div>
        </div>
    </div>

}
