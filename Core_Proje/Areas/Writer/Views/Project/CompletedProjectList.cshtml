@using EntitiyLayer.Concrete
@model List<Project>
@{
    ViewData["Title"] = "ProjectList";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}


<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Projeler</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Başlık</th>
                            <th>Tarih</th>
                            <th>Konu</th>
                            <th>Detaylar</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            @* if (item.ProjectName == "Yeni Yazı")
                        {
                        color = "badge badge-danger";
                        }
                        if (item.ProjectName == "Yeni Video")
                        {
                        color = "badge badge-warning";
                        }
                        if (item.Status == "Yeni Konferans")
                        {
                        color = "badge badge-info";
                        }
                        if (item.Status == "Yeni Workshop")
                        {
                        color = "badge badge-success";
                        }*@

                            <tr>
                                <th>@item.ProjectID</th>
                                <td>@item.ProjectName</td>
                                @*<td>@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</td>*@
                                <td><label>@item.Progress</label></td>
                                <td><a href="/Writer/Announcement/AnnouncementDetails/@item.ProjectID" class="btn btn-outline-primary btn-sm">Detaylar</a></td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

